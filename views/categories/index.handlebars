<div class="card border-secondary mb-3 text-center mx-auto" style="width: 30rem;">
    <h4 class="card-header">Category</h4>
    <div class="card-body">
        {{#each categories}}

        <a href="/categories/{{slug}}">
            <h5 class="text-center text-info">{{name}}</h5>
        </a>
        {{else}}

        {{/each}}
    </div>
</div>