<div class="card">
    <div class="card-body">
        <h4 class="text-center font-weight-bold">Category</h4><br>
        <hr>
        {{#each categories}}

        <a href="/categories/{{slug}}"><h5 class="text-center">{{name}}</h5></a>
        {{else}}

        {{/each}}
    </div>
</div>